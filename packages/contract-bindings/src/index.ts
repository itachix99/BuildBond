/**
 * Placeholder for generated TypeScript contract clients (generated via `stellar contract bindings` in Phase 6).
 */

export interface ContractBindingPlaceholder {
  version: string;
}

export const BINDINGS_VERSION = '0.1.0';
